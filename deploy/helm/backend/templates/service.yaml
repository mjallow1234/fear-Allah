apiVersion: v1
kind: Service
metadata:
  name: {{ include "fear-allah-backend.fullname" . }}
  labels:
    {{- include "fear-allah-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fear-allah-backend.selectorLabels" . | nindent 4 }}
