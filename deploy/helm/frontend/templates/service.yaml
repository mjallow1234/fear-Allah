apiVersion: v1
kind: Service
metadata:
  name: {{ include "fear-allah-frontend.fullname" . }}
  labels:
    {{- include "fear-allah-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fear-allah-frontend.selectorLabels" . | nindent 4 }}
