[pytest]
anyio_backend = asyncio
norecursedirs =
    uploads
    scripts
    .git
    .venv
    venv
    env
    Activate.ps1
    node_modules
    dist
    build
    __pycache__
addopts = -ra
python_files = test_*.py
markers =
    integration: marks integration tests
    unit: marks unit tests
    asyncio: legacy asyncio marker (mapped to anyio)
