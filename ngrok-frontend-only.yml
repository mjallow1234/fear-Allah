version: "3"
agent:
    # Set your ngrok auth token here (https://dashboard.ngrok.com/get-started/your-authtoken)
    # Never commit real tokens — store in environment or ngrok's config directory instead.
    authtoken: YOUR_NGROK_AUTH_TOKEN

tunnels:
    frontend:
        addr: 3000
        proto: http
